<h1><%= pluralize(@users.count, "User") %></h1>
<ul class="users">
  <% @users.each do |user| %>
    <li id=<%= "user-#{user.id}" %> >
      <%= link_to user.name, user %>
      created
      <%= time_ago_in_words(user.created_at) %>
      ago
    </li>
  <% end %>
</ul>